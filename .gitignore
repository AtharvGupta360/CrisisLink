# Compiled binaries
/bin/
crisislink
crisislink.exe
*.exe
*.out

# Secrets & local config — never committed (config.yaml.example is the template)
config.yaml
.env
.env.*

# Go
*.test
*.prof
vendor/

# Learning / design docs — kept local, pushed only when the project is complete
*.md

# Claude Code local (personal) settings — never published
.claude/settings.local.json

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
