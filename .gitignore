# Compiled binaries
/bin/
crisislink
crisislink.exe
*.exe
*.out

# Env & secrets — config comes from the environment, never the repo
.env
.env.*
!.env.example

# Go
*.test
*.prof
vendor/

# Learning / design docs — kept local, pushed only when the project is complete
*.md

# Claude Code local (personal) settings — never published
.claude/settings.local.json

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
