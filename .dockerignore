# Keep the build context small and secrets out of the image.
.git
.gitignore
*.md
docs/
deploy/
test/
scripts/
config.yaml          # real secrets live here (gitignored); never bake into an image
*.log
.claude/
Dockerfile
.dockerignore
